Tabs — sections of one object, 3-6 of them, sentence case.

    <Tabs tabs={["Overview","Usage","Invoices","Settings"]} active={tab} onChange={setTab}/>

The rule under the tabs is one of the five hairlines. For switching a view of the same data, use SegmentedControl instead.
