SegmentedControl — two or three short options, same data seen differently ("Table / Chart", "Day / Week / Month").

    <SegmentedControl options={["Table","Chart"]} active={view} onChange={setView}/>
