NavItem — one active item per shell.

    <NavItem icon={<Icon name="inbox"/>} label="Requests" count="3" active/>

Counts are plain text at the right edge. The nav sits on surface, separated from the page by exactly one hairline.
