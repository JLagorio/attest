Switch — ink when on, #CDD6E4 when off. Never accent: a settings page has dozens of these and the accent budget is three.

    <Switch checked={on} onChange={setOn} label="Require SSO" description="Members sign in through your identity provider"/>
