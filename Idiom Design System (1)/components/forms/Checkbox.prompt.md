Checkbox — 16px, ink when checked. Bare in table header/rows; labelled in forms.

    <Checkbox checked={all} onChange={setAll}/>
    <Checkbox checked={ok} onChange={setOk} label="Email me when a run fails"/>
