Input — labelled field. Label above, helper below, both muted.

    <Input label="Work email" placeholder="you@company.com"/>
    <Input label="Seat limit" hint="Counts every member with edit access" defaultValue="25"/>
