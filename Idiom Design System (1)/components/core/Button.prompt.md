Button — primary, secondary, quiet. Labels name the actual action ("Invite teammate", "Export CSV"), never "Submit".

    <Button variant="primary" icon={<Icon name="plus" size={15}/>}>New workspace</Button>
    <Button>Export CSV</Button>
    <Button variant="quiet">Cancel</Button>

One primary per screen — it is one of the three accent uses. Hover shifts background only; nothing here casts a shadow.
