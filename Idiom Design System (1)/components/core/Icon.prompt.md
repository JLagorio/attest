Icon — the system's only icon source: 15 stroke glyphs (1.6 weight, round caps, currentColor) copied from the source sprite. Never hand-draw new SVGs or use emoji.

    <Icon name="inbox"/>
    <Icon name="plus" size={15}/>

Names: search, home, org, box, lib, lay, clip, inbox, flag, shield, chev, help, bell, gear, plus. Color comes from CSS currentColor.
