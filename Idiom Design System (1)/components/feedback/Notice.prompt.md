Notice — the system's only banner. A tinted panel would break the no-filled-panels rule, so the badge carries the tone and space does the grouping.

    <Notice tone="problem" label="Impacted" action={<Link href="#">Review impact</Link>}>The gateway configuration moved to GW-STD-12 on Aug 19. Six inherited objectives are held at review required.</Notice>

Place it directly under the page title with space above and below, never at the top of a table.
