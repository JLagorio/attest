EmptyState — replaces the table body, keeping the header and toolbar in place.

    <EmptyState title="No invoices yet" body="Invoices appear here the first time a subscription renews." action={<Button variant="primary">Create invoice</Button>}/>

Never centre it in the viewport and never illustrate it.
