KeyValue — the body of every record surface. Values may be text, a Badge, a mono identifier or a Link.

    <KeyValue items={[{label:"Plan",value:"Scale"},{label:"Customer ID",value:<span className="t-mono">cus_8Q2fLm</span>},{label:"Owner",value:<span className="muted">Unassigned</span>}]}/>

Nulls are words ("Unassigned", "Not set") in muted, never blank cells.
